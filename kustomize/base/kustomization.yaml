# my-kustomize/base/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# base 레벨에서 사용할 리소스 파일들
resources:
  - back.yaml
  - front.yaml
  - mysql.yaml

# (선택) base 레벨에서 공통 레이블, 어노테이션, 이미지 오버라이드 등을 지정할 수도 있음
# commonLabels:
#   app.kubernetes.io/part-of: "cicd-ice"
#
# images:
#   - name: woya031/ci-ice-front
#     newTag: "1.2.8"
